import type { UIMessages } from "../types"
import { common } from "./common"
import { nav } from "./nav"

/** Composed English UI messages — the only fully-populated locale in this phase. */
export const en: UIMessages = { common, nav }
