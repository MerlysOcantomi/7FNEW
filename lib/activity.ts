// Re-export from new location — will be removed after full migration
export { logActivity, detectChanges, logChanges } from "@core/activity"
export type { ActivityType, ActivityData } from "@core/activity"
