// Re-export from new location — will be removed after full migration
export { getRequiredPortalContext } from "@core/auth/portal-context"
export type { PortalContext } from "@core/auth/portal-context"
